{
  "name": "sky-piano-sheet-maker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run build && node server.js",
    "build": "node --check app.js && node --check api/mimo-refine.js && node --check api/sheet-omr.js && node --check server.js && mkdir -p public && cp index.html styles.css app.js public/",
    "start": "node server.js",
    "vercel:dev": "npm run build && vercel dev"
  },
  "devDependencies": {
    "vercel": "latest"
  }
}
